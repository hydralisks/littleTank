# 第一章策略提示敏感性

生成时间：2026-07-14T13:11:18.130Z

采样预算：`quick`。本文档用于内部验证 strategyTips 对学习型 AI 的影响，不作为玩家侧结算评价。

## 总结

- 关键依赖：`RingingDeeps-4`
- 高依赖：`RingingDeeps-5`
- 中依赖：无
- 低依赖：`RingingDeeps-1`、`RingingDeeps-2`、`RingingDeeps-3`、`RingingDeeps-6`

## 通过率下降对比

| 关卡 | 提示信号 | 正常参考 | 不太注意 | 完全忽视 | 故意违反 | 提示依赖度 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| `RingingDeeps-1` 心核隧途 | 无 | 100% | 100%（-0pp / 0%） | 100%（-0pp / 0%） | 100%（-0pp / 0%） | 低 |
| `RingingDeeps-2` 载钢车站 | 打断/控制、法术反射 | 88% | 100%（-0pp / 0%） | 100%（-0pp / 0%） | 100%（-0pp / 0%） | 低 |
| `RingingDeeps-3` 影脉挖掘场 | 打断/控制 | 25% | 0%（-25pp / 100%） | 25%（-0pp / 0%） | 0%（-25pp / 100%） | 低 |
| `RingingDeeps-4` 失落矿坑 | 打断/控制、减伤时机、优先击杀/集火 | 38% | 25%（-13pp / 33%） | 0%（-38pp / 100%） | 0%（-38pp / 100%） | 关键 |
| `RingingDeeps-5` 矶石宝库 | 减伤时机 | 50% | 88%（-0pp / 0%） | 25%（-25pp / 50%） | 50%（-0pp / 0%） | 高 |
| `RingingDeeps-6` 怒焰裂口 | 机制联动 | 0% | 13%（-0pp / 0%） | 13%（-0pp / 0%） | 0%（-0pp / 0%） | 低 |

## 逐关说明

### RingingDeeps-1 心核隧途

- strategyTips：不要让“工作工作”压力你的队伍
- 识别信号：无
- 依赖判断：低；完全忽视 strategyTips 后通过率下降 0pp / 0%；提示依赖较低，常规学习路径基本能覆盖。
- 不太注意：不插入 strategyTips 优先构筑候选，但保留提示文本参与战术过滤；候选构筑 1 个。
- 完全忽视：清空 strategyTips，并且不插入提示优先构筑候选；候选构筑 1 个。
- 故意违反：未识别到可稳定反向验证的提示项，本模式等价于完全忽视；候选构筑 1 个。

### RingingDeeps-2 载钢车站

- strategyTips：“烛火飞弹”单次伤害很高，打断收益巨大
- 识别信号：打断/控制、法术反射
- 依赖判断：低；完全忽视 strategyTips 后通过率下降 0pp / 0%；提示依赖较低，常规学习路径基本能覆盖。
- 不太注意：不插入 strategyTips 优先构筑候选，但保留提示文本参与战术过滤；候选构筑 2 个。
- 完全忽视：清空 strategyTips，并且不插入提示优先构筑候选；候选构筑 2 个。
- 故意违反：清空 strategyTips，并排除可识别的提示对齐构筑/战术；候选构筑 2 个。

### RingingDeeps-3 影脉挖掘场

- strategyTips：“疾风连击”在施法第二段持续引导阶段中打断就不会再次释放
- 识别信号：打断/控制
- 依赖判断：低；完全忽视 strategyTips 后通过率下降 0pp / 0%；提示依赖较低，常规学习路径基本能覆盖。
- 不太注意：不插入 strategyTips 优先构筑候选，但保留提示文本参与战术过滤；候选构筑 2 个。
- 完全忽视：清空 strategyTips，并且不插入提示优先构筑候选；候选构筑 2 个。
- 故意违反：清空 strategyTips，并排除可识别的提示对齐构筑/战术；候选构筑 2 个。

### RingingDeeps-4 失落矿坑

- strategyTips：先击杀高伤害的敌人，趁着打断控制减伤等手牌还充足
- 识别信号：打断/控制、减伤时机、优先击杀/集火
- 依赖判断：关键；完全忽视 strategyTips 后通过率下降 38pp / 100%；提示可能对应关键解法或关键构筑。
- 不太注意：不插入 strategyTips 优先构筑候选，但保留提示文本参与战术过滤；候选构筑 2 个。
- 完全忽视：清空 strategyTips，并且不插入提示优先构筑候选；候选构筑 2 个。
- 故意违反：清空 strategyTips，并排除可识别的提示对齐构筑/战术；候选构筑 2 个。

### RingingDeeps-5 矶石宝库

- strategyTips：一定要记得你的“盾墙”，用它扛过敌人攻势最猛的时候
- 识别信号：减伤时机
- 依赖判断：高；完全忽视 strategyTips 后通过率下降 25pp / 50%；提示对稳定通关有明显帮助。
- 不太注意：不插入 strategyTips 优先构筑候选，但保留提示文本参与战术过滤；候选构筑 2 个。
- 完全忽视：清空 strategyTips，并且不插入提示优先构筑候选；候选构筑 2 个。
- 故意违反：清空 strategyTips，并排除可识别的提示对齐构筑/战术；候选构筑 2 个。

### RingingDeeps-6 怒焰裂口

- strategyTips：蜡烛之王的“暗影之锄”会消耗掉“蜡像”的debuff，只要保证它们命中同一目标就好
- 识别信号：机制联动
- 依赖判断：低；完全忽视 strategyTips 后通过率下降 0pp / 0%；提示依赖较低，常规学习路径基本能覆盖。
- 不太注意：不插入 strategyTips 优先构筑候选，但保留提示文本参与战术过滤；候选构筑 2 个。
- 完全忽视：清空 strategyTips，并且不插入提示优先构筑候选；候选构筑 2 个。
- 故意违反：清空 strategyTips，并排除可识别的提示对齐构筑/战术；候选构筑 2 个。
