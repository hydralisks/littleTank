# Profile V2 Balance Calibration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Update RingingDeeps manual baselines, expand automated balance profiles to nine human-like profiles, increase samples to 100 attempts, regenerate reports, and compare the new AI results with manual records.

**Architecture:** Keep the existing pass-rate-only classifier and best-build scoring mode. Extend `BalanceOperationProfile` with optional random reaction and mistake behavior fields, then let `scripts/analyzeBalance.mjs` define nine profile presets and use 100 attempts per scenario. Do not modify planner-owned workbook data under `public/designer-data`.

**Tech Stack:** TypeScript, Vitest, Node ESM scripts, current Vite/Tauri project tooling.

---

## File Structure

- Modify `src/game/balance/balanceSimulator.ts`
  - Add sampled per-decision reaction delay and concrete mistake behavior.
  - Preserve existing fixed-profile compatibility for current tests.
- Modify `src/game/balance/balanceSimulator.test.ts`
  - Cover sampled profile fields and concrete mistake effects without adding indirect HP/pressure scoring.
- Modify `scripts/analyzeBalance.mjs`
  - Expand profiles from 3 to 9 and change attempts from 12 to 100.
- Modify `src/game/balance/balanceReport.ts`
  - Update manual baselines for RingingDeeps-1 and RingingDeeps-2.
- Modify docs under `docs/balance/`
  - Update manual baseline and scoring system notes after report regeneration.
- Generated: `reports/balance/latest.md`, `reports/balance/latest.json`
  - Regenerated by `npm run analyze:balance`.

## Tasks

- [ ] Task 1: Update manual baseline tests and data for RD-1 easy, RD-2 balanced.
- [ ] Task 2: Add profile-v2 simulator tests for sampled reaction and mistake fields.
- [ ] Task 3: Implement profile-v2 simulator behavior.
- [ ] Task 4: Expand analyze profiles to 9 and set 100 attempts.
- [ ] Task 5: Regenerate report and update docs.
- [ ] Task 6: Run targeted tests, full tests, and production build.

## Verification Commands

- `npm test -- src/game/balance/balanceReport.test.ts src/game/balance/balanceSimulator.test.ts`
- `npm run analyze:balance`
- `npm test`
- `npm run build`

## Notes

- No git commit will be attempted because this workspace has no `.git` directory.
- The scoring label remains based only on pass rates. No player HP, party HP, or pressure-ratio fields may affect labels.
