# 熊 T WF6 人工复测调优实施计划

> **供执行代理使用：** 按任务顺序执行，并使用复选框（`- [ ]`）跟踪进度。

**目标：** 应用已确认的熊 T 技能、天赋、解锁和临时最大生命值改动，准备全解锁本地演示存档，并重新运行 WF6 平衡分析。

**架构：** 策划数值和解锁快照保留在三份策划工作簿中，只通过稳定 ID 定向修改。运行时复用现有熊 T 怒气、禁疗、状态到期和最大生命值比例变化流水线。

**技术栈：** TypeScript、Vitest、SheetJS/XLSX、React/Vite、浏览器 localStorage、现有平衡模拟器。

---

### 任务 1：Lock the approved data and unlock contracts

**文件：**
- 修改：`src/game/playerClasses/bearTDesign.test.ts`
- 修改：`src/game/playerClasses/bearTChallengeIntegration.test.ts`

- [ ] Change workbook assertions to Ironfur 20 rage, Barkskin 30%, Savage Focus 7, Mark of the Wild 20%, Iron Thorns 15%, Bark Dispelling 10%, Regenerative Bond cost 1, Water and Fire Immunity 20%/10% with three stacks, and Sleeper duration/max-health values.
- [ ] Change the cumulative Bear skill order so Sleeper is the tenth skill and Survival Instincts is the thirteenth skill.
- [ ] Run the two tests and confirm failures reference the old workbook values and old unlock order.

### 任务 2：Lock runtime behavior

**文件：**
- 修改：`src/game/playerClasses/bearTSkillRuntime.test.ts`

- [ ] Assert Savage Focus grants 7 additional Mangle rage only.
- [ ] Assert Mark of the Wild and Iron Thorns use 20% and 15% values.
- [ ] Assert Bark Dispelling removes dispellable debuffs and heals 10% max health, while Murloc Watch suppresses that healing.
- [ ] Assert Water and Fire Immunity keeps three Ironfur stacks at 20% physical and 10% magic per stack.
- [ ] Assert Sleeper lasts 10000ms, grants 25 rage after its existing cost, adds 25% max health, and preserves health percentage on apply and expiry.
- [ ] Run the runtime test and confirm the new assertions fail for the old implementation.

### 任务 3：Patch designer workbooks and source scripts

**文件：**
- 创建：`scripts/applyBearTWf6ManualRetestTuning.mjs`
- 修改：`scripts/rebalanceBearTWf6Defense.mjs`
- 修改：`scripts/addBearTPartyOffenseTalents.mjs`
- 修改：`scripts/addBearTPlayerBuild.mjs`
- 修改：`scripts/syncBearTCampaignSnapshots.mjs`
- 修改：`public/designer-data/player_build.xlsx`
- 修改：`public/designer-data/stage_content.xlsx`
- 修改：`public/designer-data/challenge_stage_content.xlsx`

- [ ] Implement exact stable-ID workbook updates for all approved values and descriptions.
- [ ] Swap Sleeper and Survival Instincts in mainline unlock rows and challenge cumulative snapshots while preserving all unrelated IDs.
- [ ] Update source scripts so rerunning an earlier Bear patch cannot restore superseded values.
- [ ] Run only `node scripts/applyBearTWf6ManualRetestTuning.mjs`; do not run workbook generators.

### 任务 4：Implement runtime changes

**文件：**
- 修改：`src/game/data/playerTalentLogicRegistry.ts`
- 修改：`src/game/encounter/playerSkillRuntimeRegistry.ts`
- 修改：`src/game/encounter/encounterFactory.ts`

- [ ] Read the revised talent values from the workbook with matching fallback values.
- [ ] Route Bark Dispelling's expiry heal through the existing healing multiplier and suppression logic, then retain the existing dispel.
- [ ] Add Sleeper to the shared proportional temporary-max-health status set and grant 25 rage through `applyBearRageGain` on successful activation.
- [ ] Keep Water and Fire Immunity at the normal three-stack cap and derive physical reduction from Ironfur plus magic reduction from the talent.
- [ ] Run focused tests until all approved behavior passes.

### 任务 5：Verify the project

**文件：**
- No additional files expected.

- [ ] Run `npm test`.
- [ ] Run `npm run lint`.
- [ ] Run `npm run build`.
- [ ] Run `npm run validate:designer-data`.
- [ ] Run `git diff --check` and accept only existing line-ending warnings.

### 任务 6：Prepare the demo and run WF6 analysis

**文件：**
- 创建或更新：`reports/balance/熊T-WF6人工复测调优报告.md`

- [ ] Start Vite on an unused localhost port.
- [ ] Write a valid fully unlocked `littleTank.save.2.dev` through the browser, using legal class defaults and all current campaign/challenge IDs.
- [ ] Open the stage-select demo for manual WF6 testing.
- [ ] Run `npm run analyze:balance -- --stage=WestFall-6 --classes=druid_bear_t --budget=full` concurrently with manual testing.
- [ ] Validate that the report contains exactly one `WestFall-6 / druid_bear_t` stage and summarize deltas against the prior baseline.
