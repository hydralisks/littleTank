# 报告与开发文档中文化实施计划

> **供执行代理使用：** 按任务顺序执行；修改汇总脚本时使用测试驱动开发，提交前执行完整验证。

**目标：** 中文化本轮开发文档和最终评测报告，更新全部引用，并提交推送当前工作区。

**架构：** 历史文档保持不动，仅重命名本轮7月25日至30日开发周期涉及且尚未推送的开发文档。汇总脚本继续把原始产物写入被忽略的平衡报告目录，同时把 Markdown 报告写入受 Git 管理的 `docs/reports/`。

**技术栈：** Markdown、Node.js、TypeScript、Vitest、Git。

---

### 任务 1：锁定中文报告输出契约

**文件：**
- 修改：`src/game/balance/fullStageClassComparison.test.ts`
- 修改：`scripts/buildFullStageClassComparison.mjs`

- [ ] **步骤 1：编写失败测试**

测试读取汇总脚本，断言脚本包含中文原始报告基名、`docs/reports` 目录和中文可审阅报告名。

- [ ] **步骤 2：验证测试按预期失败**

执行：`npm test -- --run src/game/balance/fullStageClassComparison.test.ts`

预期：因脚本仍使用英文报告基名且未写入 `docs/reports` 而失败。

- [ ] **步骤 3：实现中文双落点输出**

将原始报告基名改为 `熊T与战士T全关卡完整预算对比-2026-07-30`，创建 `docs/reports` 并写入 `熊T与战士T全关卡完整预算对比报告-2026-07-30.md`。

- [ ] **步骤 4：验证测试通过并生成报告**

执行：`npm test -- --run src/game/balance/fullStageClassComparison.test.ts`

执行：`node --experimental-strip-types --loader ./scripts/ts-extension-loader.mjs scripts/buildFullStageClassComparison.mjs`

预期：测试通过，中文名原始报告与受版本控制报告均生成。

### 任务 2：中文化本轮开发文档

**文件：**
- 重命名：当前工作区中 2026-07-25 至 2026-07-30 新增或修改的计划、设计文档
- 重命名：职业扩展交接文档，使其使用中文文件名
- 修改：所有引用上述旧路径的 Markdown 和脚本说明

- [ ] **步骤 1：执行限定范围的文件重命名**

日期前缀和目录保持不变，主题部分改为中文；不处理历史未触碰文件。

- [ ] **步骤 2：更新交叉引用和正文模板词**

更新旧路径引用，并将本轮计划文档顶部的 `Goal`、`Architecture`、`Tech Stack` 等说明改为中文。

- [ ] **步骤 3：扫描残留旧路径**

执行：使用 `rg` 扫描本轮已替换的旧英文文档主题和旧英文报告基名。

预期：没有旧开发文档路径或旧报告文件名残留。

### 任务 3：记录交付并验证

**文件：**
- 修改：`开发更新日志.md`
- 创建：`docs/reports/熊T与战士T全关卡完整预算对比报告-2026-07-30.md`

- [ ] **步骤 1：记录中文化和全量评测结论**

在更新日志中记录 21 关、42 份职业结果、主要风险关和验证结果。

- [ ] **步骤 2：运行完整验证**

执行：`npm test`

执行：`npm run lint`

执行：`npm run build`

执行：`npm run validate:designer-data`

执行：`git diff --check`

预期：测试、lint、构建和策划表校验通过，策划表警告为 0，差异检查无错误。

- [ ] **步骤 3：检查提交范围**

执行：`git status --short --branch`

执行：`git diff --stat`

预期：当前工作区改动全部属于本轮累计熊 T、战斗 UI、平衡评测和中文文档交付。

### 任务 4：提交并推送

- [ ] **步骤 1：同步远程状态**

执行：`git fetch origin`

预期：确认 `origin/main` 与本地提交关系，无未处理的远程分叉。

- [ ] **步骤 2：提交当前工作区**

执行：`git add -A`

执行：`git commit -m "feat: complete bear tank balance baseline"`

- [ ] **步骤 3：推送并核验**

执行：`git push origin main`

执行：`git status --short --branch`

预期：本地 `main` 与 `origin/main` 同步，工作区干净。
